# ===================================================================
# MediAssist-AI TESTING / CI Profile Configuration (test)
# ===================================================================

# PostgreSQL Database (Test Container / Test DB)
spring.datasource.url=jdbc:postgresql://localhost:5432/mediassist_test_db
spring.datasource.username=postgres
spring.datasource.password=postgres_secure_2026
spring.datasource.driver-class-name=org.postgresql.Driver

spring.datasource.hikari.maximum-pool-size=5

# Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# Redis
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Security & CORS
app.jwt.secret=test_super_secret_jwt_key_mediassist_telehealth_platform_2026_very_long_secure_string_needs_256_bits!
app.cors.allowed-origins=http://localhost:5173

# Logging Level
logging.level.root=WARN
logging.level.com.mediassist=INFO
